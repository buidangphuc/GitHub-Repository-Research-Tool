services:
  app:
    volumes:
      - .:/app
    command: ["uv", "run", "uvicorn", "main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"]

  research-worker:
    volumes:
      - .:/app
    command: ["uv", "run", "python", "-m", "app.research.worker.sqs_worker"]
